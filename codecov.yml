coverage:
  status:
    project:
      default:
        target: 75%     # cobertura total mínima
    patch:
      default:
        target: 10%     # cobertura mínima en líneas nuevas/modificadas

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false
